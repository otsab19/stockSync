fix: integrations page works in both Supabase and browser modes
- Remove the "switch to browser" gate from Trading212 and eToro sync
  cards — both now show the API key form regardless of backend mode
- In Supabase mode, credentials are persisted to both browser IndexedDB
  AND the Supabase profiles table (for cron/push notifications)
- Add /api/credentials route (GET/POST/DELETE) to manage broker keys
  in the Supabase profiles table
- Change client-factory to always use IndexedDB for dashboard display
  (Supabase mode difference is only push notifications via cron)
- Dashboard works identically in both modes: sync from integrations
  page, view on dashboard, get push alerts only in Supabase mode
